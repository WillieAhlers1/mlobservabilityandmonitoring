# Mapping definition for webhook connector — alert events
version: "1"

applies_to:
  source_connector: webhook
  event_type: alert

entity_resolution:
  strategy: lookup
  match_fields:
    - source: source_entity_ref
      target: entity_aliases.alias_value
  on_no_match: reject

field_mappings:
  - source: payload.severity
    target: alerts.severity
    transform: identity

validation_rules:
  - rule: not_null
    field: severity

target_table: alerts
